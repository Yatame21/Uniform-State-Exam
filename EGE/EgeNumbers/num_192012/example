# 1 куча
def f(s,m):
    if s >=29 : return m % 2 == 0
    if m == 0: return 0
    h= [f(s+1,m-1),f(s*2,m-1)]
    return any(h) if m%2 !=0 else all(h) # all - любой(всегда для 20 и 21)
    return any(h) if m%2 !=0 else any(h) # any - неудачный 

print([s for s in range(1,29) if f(s,2)])
print([s for s in range(1,29) if not f(s,1) and f(s,3)])
print([s for s in range(1,29) if not f(s,2) and f(s,4)])